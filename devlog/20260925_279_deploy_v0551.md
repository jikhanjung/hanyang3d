# 279 — v0.5.51 배포

2026-09-25 dolfinid에 경회루 2층·향원정 다리([276](20260925_276_gyeonghoeru_hyangwonjeong_walk.md)), 을미사변 길가 시작·저잣거리 웅성거림·말 알림과 채팅창 흐려짐([277](20260925_277_eulmi_roadside_market_crowd_chat_fade.md)), 1907년 운현궁([278](20260925_278_unhyeongung_1907.md))을 배포했다. 멀티플레이 v0.5.31 유지.

- 빌드 revision `5295c38`, digest `sha256:781575b03b9695b8b5b29beafe8ea712ec66e4391974f372520c6496ae189023`. 이미지 테스트 97개 통과. 개발 호스트 m710q에서 빌드·push(빌드 스크립트는 push하지 않아 `docker push`를 따로 실행), 운영에서는 데이터 묶음 검증·pull·교체만 수행. 호스트 묶음은 v0.5.50과 같아 풀지 않았다.
- migration 없음. 동기화: 추가 1(`building unhyeongung-1907`), 갱신·충돌 0. 새 건물의 안내 문단(1907년 운현궁)은 동기화가 새로 만들었고 공개 `/guide/`에 보인다. 보고된 '안내 문단 차이 69'는 운영 DB 쪽 문단 제목 앵커(`{#… (추정)}`) 등 예전부터 쌓인 편집 차이로, 동기화가 설계대로 보존한 것이며 이번 변경과 무관함을 운영 컨테이너에서 확인했다.
- 설정 백업 `backups/config/pre-v0.5.51/`(`.env`·`.env.django`·compose·`deploy.sh`). 웹·멀티플레이 healthy.
- 운영 확인(계정 생성 없음): 공개 `/1907/`에 `unhyeongung-1907`, `landmarks1907.js`에 운현궁 모형과 향원정 계단 코드, `walk_chat.js`에 흐려짐 코드, `events/crowd.js` 제공(200), `/events/eulmi/`에 `crowd` 모듈과 `free_until_at` 반영. 동작은 기록 276~278의 임시 DB 브라우저 검사로 확인했다.
