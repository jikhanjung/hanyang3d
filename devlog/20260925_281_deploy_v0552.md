# 281 — v0.5.52 배포

2026-09-25 dolfinid에 시점 가림 방지·을미사변 골목 시작·경회루 다리·채팅 엔터([280](20260925_280_camera_occlusion_eulmi_alley_chat_enter.md))를 배포했다. 멀티플레이 v0.5.31 유지.

- 빌드 revision `706b932`, digest `sha256:63ff64db7e99879ca728b7773e98ba15e6de6c4d2c488bd8c536bbaf01d401a4`. 이미지 테스트 97개 통과. m710q에서 빌드·push, 운영에서는 데이터 묶음 검증·pull·교체만 수행. 호스트 묶음은 v0.5.51과 같아 풀지 않았다.
- migration 없음, 콘텐츠 DB 변경 없음(회상 정의는 이미지 안 파일). 설정 백업 `backups/config/pre-v0.5.52/`. 웹·멀티플레이 healthy.
- 운영 확인(계정 생성 없음): 공개 `first_person.js`에 시점 가림 검사(`nearestOccluder`), `walk_chat.js`에 흐려질 때만 전환하는 스타일, `events/shadow.js`에 등장 지연(`delay_s`), `/events/eulmi/`에 골목 안 야경꾼 위치(531, 2228)와 7초 지연이 반영됨. 동작은 기록 280의 임시 DB 브라우저 검사로 확인했다.
