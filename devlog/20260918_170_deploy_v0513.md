# 170 — v0.5.13 종로 전차 배포

사용자 요청으로 m710q에서 빌드한 이미지를 Docker Hub에 push하고 dolfinid `/srv/hanyang3d`에 배포했다.

- 웹: `honestjung/hanyang3d:v0.5.13`
- digest: `sha256:9b67b602fcee30c1f9d9c4cf5d97484a9d07b1df2e67ca720d0ee1f37a1d75c9`
- 멀티플레이: v0.5.9 유지.
- 1907년 종로 전차·레일·가선, 전차 보기, 보행자 회피와 차량 정지·재출발, 한영 안내와 출처 추가.

첫 이미지 검사에서 신규 리소스 두 개 때문에 기존 개수 assertion(127)이 실패했다. 예상 개수를 129로 갱신한 후 `bash deploy/build.sh v0.5.13 --web-only` 재실행: 테스트 63개, HTTP·누락 및 버전 오류 차단, DB 편집 보존·비덮어쓰기 import·검증 백업 모두 통과. 전송 묶음 체크섬과 push/pull digest 일치 확인.

설정 백업: `/srv/hanyang3d/backups/config/pre-v0.5.13/`. 웹 중지 후 무결성 검증 DB 백업: `/srv/hanyang3d/backups/content/pre-deploy/content_v0.5.13_20260918_061244.sqlite3`. migration 없음. 콘텐츠 추가·갱신·충돌 모두 0, 기존 안내 문단 차이 55는 보존. `.env.django`와 릴리스 변수 외 `.env`의 동일성을 확인했다. 호스트 스크립트·Compose 해시도 로컬과 동일하여 교체하지 않았다.

웹 v0.5.13 및 멀티플레이 v0.5.9 healthy, 이미지 리소스 113개. 공개 HTTPS health 정상. 공개 1907년 PC·390px 모바일에서 전차 표시, 숨김 정지, 왕복, 지도 투명도 변경 시 높이 불변, 운행 구간 건물·성벽 충돌 없음, 회피·정지·재출발 검증 통과. JavaScript 오류 없음.

직전 v0.5.12로 DB 복원 없이 코드 롤백 가능. Git commit/push는 수행하지 않았다. 배포 후 서버 여유 공간 1.5GB로 확인했으며 이번에는 정리를 수행하지 않았다.
