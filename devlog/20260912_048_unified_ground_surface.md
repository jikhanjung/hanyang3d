# 048 — 지면·지도·도로 높이 통일과 투명도 독립

2026-09-12. 지도 불투명도를 0%로 바꾸면 물체가 내려가고 도로가 지표면 위에 떠 보이는 문제를 사용자 요청으로 수정했다. 047의 눈높이 수정과 함께 배포한다.

## 하나의 지면

원도 표면을 고도보다 4m 올리던 오프셋과 도로의 추가 0.7m를 제거했다. 기존에는 고도 격자와 원도 격자가 달라 같은 위치에서도 삼각형 보간 높이가 달랐으므로, 단순 상수 제거 대신 FABDEM 삼각형을 공통 지면으로 사용하도록 변경했다.

기존 TPS 원도 삼각형을 수평 위치로 색인하고, FABDEM 꼭짓점의 원도 UV를 역으로 보간한다. 지형·지도는 같은 position/normal 속성을 사용하며 도로도 정확히 같은 높이 배열을 사용한다. 지도 범위 밖 삼각형은 이미지·도로에서 제외하고 고도 지형만 표시한다. 원도 경계에 걸친 삼각형은 제외하므로 지도 가장자리에 지형 격자 한 칸 이내의 미세한 잘림이 있을 수 있다.

실제 높이는 올리지 않고 polygon offset과 표시 순서로 겹침을 처리한다. 하천 절삭은 공통 지면에 한 번 적용하며, 지도용 수면 오프셋 4m도 제거했다. 물 수면·다리 구조는 지형 위의 별도 물체로 유지한다.

## 투명도와 눈높이

지도 불투명도 슬라이더는 이미지 재질만 변경한다. 주택·사람·수목의 지면 기준을 전환하지 않으며 0%에서도 FABDEM 지면과 모든 배치 위치가 유지된다. 1인칭·일반 탐색도 이미지 투명도와 무관한 공통 지면을 따른다. 눈높이는 공통 지면 위 1.65m다.

## 확인

브라우저 검사에 지형·지도·도로 전체 꼭짓점 높이 오차가 0인지 확인하는 검사를 추가했다. 불투명도 0/1/50/100% 왕복 시 주택·수목·보행자 행렬·카메라가 유지되는지 확인한다. 높이 배율 1/1.5/2배, 원도·도로 표시/숨김에서 실제 메시 광선 교차로 눈높이를 검사한다.

최종 로컬 브라우저 검사 통과: 전체 꼭짓점의 지면·지도·도로 높이 차이 0m, 지도 0/1/50/100% 왕복 시 배치·카메라 불변, 높이 배율과 지도·도로 전환에서 눈높이 1.6543–1.6586m, 다리 4개 접지, PC·모바일 조작, 출처 페이지 이동을 확인했다. 공유 법선을 부분 지도 메시에서 재계산하면 지도 바깥 조명이 검게 되는 문제를 시각 검사에서 발견해, 전체 지면에서만 법선을 계산하도록 수정하고 재검증했다. 최종 화면과 법선 길이 검사도 통과했다.

최종 이미지: `honestjung/hanyang3d:v0.0.6`, digest `sha256:283f55eab25f176d7209c287d479893c21985a817ce1f376d54186abb389f4bb`.

운영 dolfinid에 v0.0.6 배포 완료. 컨테이너 healthy, HTTPS health v0.0.6, 출처 페이지 HTTP 200을 확인했다. 운영 JavaScript와 FABDEM 파일 SHA-256이 로컬 브라우저에서 검증한 파일과 일치한다.
