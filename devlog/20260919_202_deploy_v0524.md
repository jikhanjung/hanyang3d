# 202 — v0.5.24 배포와 내부 장면 데이터 연결 교정

기록 199–201의 육조거리 민가 제외·단성사 동쪽 재배치/서향 입구, 장면 데이터 DB 전환, 걷기 HUD·액션바·16칸 봇짐·채팅·PC 미니맵 확대·포커스 상실 후 이동 유지를 배포했다.

## v0.5.23 시도와 복구

웹 검증 71개, DB 편집 보존·백업, 멀티플레이 검사 통과 후 게시했다. 운영 0007_scene_dataset 마이그레이션과 5개 데이터셋 초기화, 단성사 1건 갱신은 성공했으나 멀티플레이 내부 API 요청이 HTTP 400으로 거부됐다. Node fetch가 사용자 지정 Host 헤더를 보존하지 않아 Django의 허용 호스트 검사에 걸렸다. 배포 스크립트가 자동으로 웹·멀티플레이 v0.5.22로 복귀했으며 둘 다 healthy임을 확인했다. DB는 복원하지 않고 최신 상태를 보존했다.

검증된 배포 전 DB 백업: `/srv/hanyang3d/backups/content/pre-deploy/content_v0.5.23_20260919_062553.sqlite3`. 설정은 `/srv/hanyang3d/backups/config/pre-v0.5.23/`에 보존했다.

## 수정과 재검증

Node http/https 요청으로 내부 Host: localhost를 명시적으로 전달하도록 수정했다. 15초 시간 제한과 2MB 응답 상한을 적용했다. 회귀 테스트의 모의 서버도 Host가 다르면 400을 반환하게 했으며 Docker 이미지 검사에 추가했다. 운영 허용 호스트나 비밀 설정을 완화하지 않았다. v0.5.23 태그는 덮어쓰지 않고 웹·멀티플레이 v0.5.24를 새로 빌드했다. 빌드 revision은 `eae2b57`이다.

Django 71개, 멀티플레이 기존 13개와 내부 경로 회귀 1개, 이미지 실제 연결·채팅·접속 제한·시대 분리 및 DB 보존 검사를 통과했다. 서버 교체 전 읽기 전용 DB를 연결한 임시 웹 컨테이너로 운영 내부 연결을 검사해 routeKey `7fd23b91` 일치를 확인했다.

## 운영 적용 결과

- 웹·멀티플레이 모두 v0.5.24 healthy. 공개 HTTPS 상태 확인도 정상.
- 웹 digest: `sha256:1c06946b66204d662bb46f8b1843fe99329627f6faf8d72b237b02ebf0784aa2`.
- 멀티플레이 digest: `sha256:0c9df6ffc3aa8fcc1b6bc8e7facf501518f6c56bd89344ec2ae0ce7f6c764c64`.
- 재배포 전 검증 백업: `/srv/hanyang3d/backups/content/pre-deploy/content_v0.5.24_20260919_121556.sqlite3`.
- 재배포 설정 백업: `/srv/hanyang3d/backups/config/pre-v0.5.24/`.
- 0007 마이그레이션·5개 데이터셋은 이전 시도에서 적용됐으므로 재배포는 추가/갱신/충돌 모두 0. 기존 장면 값 보존.
- `.env.django` 및 버전/프로필 이외 `.env` 설정 보존 확인. 공개 장면 API 5종의 응답과 ETag 정상.
- 운영: https://hanyang3d.nopeoplestime.info/1907/

앞으로 지원하는 장면 데이터는 `update_data.sh`로 이미지 교체 없이 갱신할 수 있다. 보행 경로 변경은 멀티플레이 재시작이 필요하다. v0.5.23은 배포 실패 버전으로 재사용하지 않는다.

공개 브라우저 검사: PC 1100×900·모바일 390×844 모두 도로 20개, 민가 1,600동(판독점 29동), 단성사 [1392,2222]/yaw 270°, 성당 실내 이름 숨김과 민가 표시 전환을 확인했다. 1750년 기본 지도도 정상 로드됐다. 건물 187·이야기 49개 유지, 장면 데이터셋 5개를 확인했다. 운영 계정을 새로 만들거나 거래 데이터를 변경하지 않았다.
