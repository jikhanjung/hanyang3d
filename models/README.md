# 3D 자산

`landmarks/`는 개별 건축물 작업, `procedural/`은 생성 규칙·매개변수, `generated/`는 재생성 가능한 출력입니다. 생성 출력은 기본적으로 Git에서 제외합니다. 대용량 작업 원본은 외부 저장소 방침 확정 후 관리합니다.

모든 모델은 GIS 객체와 근거를 manifest로 연결합니다. [3D 복원 규칙](../docs/reconstruction_rules.md)을 따릅니다. 현재 모델은 없습니다.
